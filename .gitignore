.playwright-mcp/
.DS_Store

# Боевые записи — хранятся только на сервере, деплой их не трогает
data/bookings.json
data/telegram.log

# Локальные секреты/переопределения (прокси, токены) — только на сервере
config/config.local.php
