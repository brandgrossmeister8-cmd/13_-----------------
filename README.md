# Форма записи на диагностику с админ-панелью

Система онлайн-записи с административной панелью для управления бронированиями и расписанием.

## Возможности

### Для пользователей:
- Интерактивный календарь с отображением доступных дат
- Выбор времени из доступных слотов
- Валидация всех полей формы
- Отправка данных на сервер
- Уведомления в Telegram

### Для администратора:
- Просмотр календаря с цветовой индикацией:
  - 🟢 Зеленый - все слоты свободны
  - 🟡 Желтый - частично занято
  - 🔴 Красный - все слоты заняты
  - ⚫ Серый - день заблокирован
- Управление слотами времени (блокировка/разблокировка)
- Блокировка целых дней
- Просмотр и удаление записей
- Защита паролем

## Установка

### 1. Настройка конфигурации

Откройте файл `config/config.php` и настройте параметры:

```php
// Изменить пароль администратора
// Для генерации хеша выполните в терминале:
// php -r "echo password_hash('ваш_новый_пароль', PASSWORD_BCRYPT);"
define('ADMIN_PASSWORD_HASH', 'ваш_хеш');

// Настроить Telegram бота (если нужны уведомления)
define('TELEGRAM_BOT_TOKEN', 'ваш_токен_бота');
define('TELEGRAM_CHAT_ID', 'ваш_chat_id');
```

#### Получение Telegram токенов:

1. **Bot Token:**
   - Напишите @BotFather в Telegram
   - Отправьте команду `/newbot`
   - Следуйте инструкциям
   - Скопируйте полученный токен

2. **Chat ID:**
   - Напишите вашему боту любое сообщение
   - Напишите @userinfobot и отправьте `/start`
   - Скопируйте ваш ID

### 2. Настройка рабочих часов

В `config/config.php` можно изменить рабочие часы и дни:

```php
// Рабочие часы
define('WORKING_HOURS', [
    '10:00', '11:00', '12:00', '13:00',
    '14:00', '15:00', '16:00', '17:00'
]);

// Рабочие дни (1 = Пн, 7 = Вс)
define('WORKING_DAYS', [1, 2, 3, 4, 5]); // Пн-Пт
```

### 3. Проверка прав доступа

Убедитесь, что директория `data/` доступна для записи:

```bash
chmod 755 data/
chmod 644 data/bookings.json
```

## Использование

### Доступ к админ-панели

Есть два способа открыть админ-панель:

1. **Горячие клавиши:** `Ctrl + Shift + A`
2. **URL параметр:** `index.html?admin=1`

**Пароль по умолчанию:** `admin123` (⚠️ ОБЯЗАТЕЛЬНО ИЗМЕНИТЕ!)

### Работа с админ-панелью

#### Вкладка "Календарь"
1. Выберите нужную дату в календаре
2. Увидите все слоты времени для этой даты
3. Кликните на слот чтобы заблокировать/разблокировать
4. Используйте кнопку "Заблокировать весь день" для блокировки всех слотов

#### Вкладка "Записи"
1. Просмотр всех бронирований
2. Удаление записей кнопкой "Удалить"

## Структура проекта

```
.
├── index.html              # Главная страница с формой и админ-панелью
├── send.php                # Обработка отправки формы
├── api/
│   ├── auth.php           # Авторизация администратора
│   ├── availability.php   # Проверка доступности дат/слотов
│   ├── bookings.php       # CRUD операции с записями
│   └── helpers.php        # Вспомогательные функции
├── config/
│   ├── config.php         # Конфигурация
│   └── .htaccess          # Защита директории
├── data/
│   ├── bookings.json      # Хранилище данных
│   └── .htaccess          # Защита директории
└── README.md              # Документация
```

## API Endpoints

### Публичные (без авторизации)

#### `GET /api/availability.php?month=YYYY-MM`
Получить доступность для месяца.

**Ответ:**
```json
{
  "success": true,
  "dates": {
    "2026-01-25": {
      "status": "partial",
      "available_slots": ["10:00", "11:00"],
      "total_slots": 8
    }
  }
}
```

#### `GET /api/availability.php?date=YYYY-MM-DD`
Получить доступные слоты для даты.

**Ответ:**
```json
{
  "success": true,
  "date": "2026-01-25",
  "blocked": false,
  "slots": [
    {
      "time": "10:00",
      "available": true
    },
    {
      "time": "11:00",
      "available": false,
      "reason": "Занято",
      "booked": true
    }
  ]
}
```

#### `POST /send.php`
Создать новую запись.

**Тело запроса:**
```json
{
  "name": "Иван Петров",
  "phone": "+7 (999) 123-45-67",
  "email": "ivan@mail.ru",
  "date": "25.01.2026",
  "time": "10:00",
  "problem": "Описание проблемы"
}
```

### Административные (требуют авторизации)

#### `POST /api/auth.php?action=login`
Вход в систему.

**Тело запроса:**
```json
{
  "password": "пароль"
}
```

#### `GET /api/auth.php?action=check`
Проверка статуса авторизации.

#### `GET /api/auth.php?action=logout`
Выход из системы.

#### `GET /api/bookings.php`
Получить все бронирования.

#### `DELETE /api/bookings.php?id=1`
Удалить бронирование.

#### `POST /api/bookings.php?action=block_slot`
Заблокировать слот.

**Тело запроса:**
```json
{
  "date": "2026-01-25",
  "time": "10:00",
  "reason": "Перерыв"
}
```

#### `POST /api/bookings.php?action=unblock_slot`
Разблокировать слот.

#### `POST /api/bookings.php?action=block_date`
Заблокировать весь день.

#### `POST /api/bookings.php?action=unblock_date`
Разблокировать день.

## Безопасность

- ✅ Пароль хешируется с использованием bcrypt
- ✅ Директории `/config/` и `/data/` защищены через .htaccess
- ✅ Все административные операции требуют авторизации
- ✅ Валидация всех входных данных
- ✅ Атомарная запись в JSON с блокировкой файла
- ✅ PHP сессии с HttpOnly и SameSite

## Устранение неполадок

### Проблема: "Ошибка соединения с сервером"
- Проверьте, что PHP работает корректно
- Убедитесь, что файлы в директории `api/` доступны

### Проблема: "Файл не найден" для bookings.json
- Проверьте права доступа к директории `data/`
- Убедитесь, что `data/bookings.json` существует

### Проблема: Telegram уведомления не приходят
- Проверьте правильность токена бота и chat ID в `config/config.php`
- Убедитесь, что вы написали боту хотя бы одно сообщение
- Проверьте, что на сервере разрешены исходящие HTTP запросы

### Проблема: Не могу войти в админ-панель
- Проверьте правильность пароля
- Убедитесь, что хеш в `config/config.php` соответствует вашему паролю
- Проверьте, что PHP сессии работают корректно

## Генерация нового пароля

Чтобы сгенерировать хеш для нового пароля, выполните в терминале:

```bash
php -r "echo password_hash('ваш_новый_пароль', PASSWORD_BCRYPT) . PHP_EOL;"
```

Скопируйте полученный хеш и вставьте в `config/config.php`:

```php
define('ADMIN_PASSWORD_HASH', 'полученный_хеш');
```

## Лицензия

© 2026 ИП ОСМАЕВА МАРГАРИТА ВИТАЛЬЕВНА
