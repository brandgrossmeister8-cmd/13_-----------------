# Быстрый старт

## ⚡ Запуск за 3 шага

### Шаг 1: Установите пароль администратора

Сгенерируйте хеш для нового пароля:

```bash
php -r "echo password_hash('ваш_пароль', PASSWORD_BCRYPT) . PHP_EOL;"
```

Вставьте полученный хеш в `config/config.php`:

```php
define('ADMIN_PASSWORD_HASH', 'полученный_хеш');
```

### Шаг 2: Настройте Telegram (опционально)

Если хотите получать уведомления в Telegram, следуйте инструкциям в файле [TELEGRAM_SETUP.md](TELEGRAM_SETUP.md)

Если не нужны уведомления - оставьте токены пустыми в `config/config.php`:

```php
define('TELEGRAM_BOT_TOKEN', '');
define('TELEGRAM_CHAT_ID', '');
```

### Шаг 3: Проверьте права доступа

```bash
chmod 755 data/
chmod 644 data/bookings.json
```

## ✅ Готово!

Откройте `index.html` в браузере.

### Для пользователей:
- Выберите дату и время
- Заполните форму
- Нажмите "Записаться на диагностику"

### Для администратора:
- Нажмите `Ctrl + Shift + A` или откройте `index.html?admin=1`
- Введите пароль
- Управляйте записями и расписанием

## 📋 Что дальше?

1. **Настройте рабочие часы** в `config/config.php` (по умолчанию: 10:00-17:00, Пн-Пт)
2. **Заблокируйте ненужные дни** через админ-панель (праздники, отпуска)
3. **Протестируйте форму** - сделайте тестовую запись
4. **Проверьте Telegram уведомления** (если настроили)

## 🔐 Безопасность

- ✅ Директории `/config/` и `/data/` защищены через .htaccess
- ✅ Пароль хешируется с bcrypt
- ✅ Все админ-операции требуют авторизации

## ⚠️ Важно

- **ИЗМЕНИТЕ пароль по умолчанию** (`admin123`)
- **НЕ удаляйте** файлы `.htaccess` в директориях config/ и data/
- **Делайте бэкапы** файла `data/bookings.json`

## 📞 Поддержка

При возникновении проблем:
1. Проверьте раздел "Устранение неполадок" в [README.md](README.md)
2. Проверьте права доступа к файлам
3. Проверьте error_log PHP

---

**Разработано для ИП ОСМАЕВА МАРГАРИТА ВИТАЛЬЕВНА**
